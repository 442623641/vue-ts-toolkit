const chalk = require('chalk');                        // 命令行log样式
module.exports = {
    strong: chalk.bold,
    weak: chalk.dim,
    input: chalk.cyan,
    success: chalk.green,
    failure: chalk.red,
};
